namespace PMSCore;

/// <summary>
/// Enum represents Entity status
/// </summary>
public enum EntityStatus
{
    New = 0,
    Ready = 1,
    InProgress = 2,
    InReview = 3,
    Done = 4,
    Closed = 5,
    Cancelled = 6,
}